# 简历分析系统前端页面与交互设计文档

## 1. 文档说明

### 1.1 文档目的

本文档用于说明简历分析系统前端项目的页面结构、路由设计、交互流程、组件划分、状态管理、接口调用方式和前端工程结构。

本文档主要面向：

- 前端开发人员
- 后端开发人员
- 测试人员
- 项目评审人员
- 后续维护人员

### 1.2 前端技术栈

本项目采用前后端分离架构，前端建议使用以下技术栈：

| 技术         | 用途         |
| ------------ | ------------ |
| Vue 3        | 前端框架     |
| Vite         | 构建工具     |
| TypeScript   | 类型约束     |
| Vue Router   | 路由管理     |
| Pinia        | 全局状态管理 |
| Axios        | HTTP 请求    |
| Element Plus | UI 组件库    |
| ECharts      | 图表展示     |

### 1.3 前端设计目标

前端设计需要满足以下目标：

1. 页面结构清晰，适合求职者快速完成简历分析。
2. 操作流程简单，降低用户使用成本。
3. 分析报告展示直观，突出评分、匹配度和修改建议。
4. 管理后台与用户端在同一前端项目中区分路由和权限。
5. 与 FastAPI 后端 API 接口保持清晰边界。
6. 支持 Docker 部署和 Nginx 静态资源托管。

------

## 2. 前端整体架构

## 2.1 架构说明

前端作为独立 Vue 项目运行，开发环境通过 Vite Dev Server 启动，生产环境构建为静态文件，由 Nginx 托管。

开发环境访问：

```text
http://localhost:5173
```

后端 API 地址：

```text
http://localhost:8000/api
```

生产环境访问：

```text
https://your-domain.com
```

生产环境 API 地址：

```text
https://your-domain.com/api
```

## 2.2 前端与后端通信

前端通过 Axios 调用后端 RESTful API。

调用方式：

```text
Vue 页面 / 组件
  ↓
api 模块
  ↓
Axios 实例
  ↓
FastAPI 后端接口
```

## 2.3 前端模块划分

前端主要分为：

1. 登录注册模块
2. 用户工作台模块
3. 简历管理模块
4. 岗位描述模块
5. 简历分析模块
6. 分析报告模块
7. 个人中心模块
8. 管理后台模块
9. 公共组件模块
10. API 请求模块
11. 状态管理模块

------

## 3. 前端项目目录结构

推荐目录结构如下：

```text
frontend/
├── public/
│   └── favicon.ico
├── src/
│   ├── api/
│   │   ├── request.ts
│   │   ├── auth.ts
│   │   ├── user.ts
│   │   ├── resume.ts
│   │   ├── job.ts
│   │   ├── analysis.ts
│   │   ├── report.ts
│   │   ├── dashboard.ts
│   │   └── admin.ts
│   ├── assets/
│   │   ├── styles/
│   │   │   ├── index.scss
│   │   │   └── variables.scss
│   │   └── images/
│   ├── components/
│   │   ├── common/
│   │   │   ├── AppHeader.vue
│   │   │   ├── AppSidebar.vue
│   │   │   ├── AppBreadcrumb.vue
│   │   │   ├── PageContainer.vue
│   │   │   ├── EmptyState.vue
│   │   │   └── LoadingState.vue
│   │   ├── resume/
│   │   │   ├── ResumeUpload.vue
│   │   │   ├── ResumeCard.vue
│   │   │   └── ResumePreview.vue
│   │   ├── job/
│   │   │   ├── JobForm.vue
│   │   │   └── JobCard.vue
│   │   ├── report/
│   │   │   ├── ScoreCard.vue
│   │   │   ├── MatchChart.vue
│   │   │   ├── KeywordCoverage.vue
│   │   │   ├── SuggestionList.vue
│   │   │   └── RewriteExampleList.vue
│   │   └── admin/
│   ├── layouts/
│   │   ├── BlankLayout.vue
│   │   ├── UserLayout.vue
│   │   └── AdminLayout.vue
│   ├── router/
│   │   └── index.ts
│   ├── stores/
│   │   ├── auth.ts
│   │   ├── user.ts
│   │   └── app.ts
│   ├── types/
│   │   ├── auth.ts
│   │   ├── user.ts
│   │   ├── resume.ts
│   │   ├── job.ts
│   │   ├── analysis.ts
│   │   └── report.ts
│   ├── utils/
│   │   ├── auth.ts
│   │   ├── date.ts
│   │   ├── file.ts
│   │   └── format.ts
│   ├── views/
│   │   ├── auth/
│   │   │   ├── Login.vue
│   │   │   └── Register.vue
│   │   ├── dashboard/
│   │   │   └── Dashboard.vue
│   │   ├── resumes/
│   │   │   ├── ResumeList.vue
│   │   │   ├── ResumeDetail.vue
│   │   │   └── ResumeUploadPage.vue
│   │   ├── jobs/
│   │   │   ├── JobList.vue
│   │   │   ├── JobDetail.vue
│   │   │   └── JobCreate.vue
│   │   ├── analysis/
│   │   │   ├── AnalysisCreate.vue
│   │   │   └── AnalysisTaskDetail.vue
│   │   ├── reports/
│   │   │   ├── ReportList.vue
│   │   │   └── ReportDetail.vue
│   │   ├── profile/
│   │   │   └── Profile.vue
│   │   └── admin/
│   │       ├── AdminDashboard.vue
│   │       ├── UserManage.vue
│   │       ├── ResumeManage.vue
│   │       ├── AnalysisTaskManage.vue
│   │       ├── ApiLogManage.vue
│   │       └── PromptTemplateManage.vue
│   ├── App.vue
│   └── main.ts
├── Dockerfile
├── package.json
├── vite.config.ts
├── tsconfig.json
└── .env.example
```

------

## 4. 路由设计

## 4.1 路由分组

前端路由分为三类：

1. 公开路由：登录、注册。
2. 用户路由：工作台、简历、岗位、分析、报告、个人中心。
3. 管理员路由：用户管理、简历管理、分析任务管理、API 日志、Prompt 模板管理。

## 4.2 路由表设计

| 路径                    | 页面                     | 权限   | 说明            |
| ----------------------- | ------------------------ | ------ | --------------- |
| /login                  | Login.vue                | 公开   | 登录页          |
| /register               | Register.vue             | 公开   | 注册页          |
| /                       | Dashboard.vue            | 用户   | 用户工作台      |
| /dashboard              | Dashboard.vue            | 用户   | 用户工作台      |
| /resumes                | ResumeList.vue           | 用户   | 简历列表        |
| /resumes/upload         | ResumeUploadPage.vue     | 用户   | 上传简历        |
| /resumes/:id            | ResumeDetail.vue         | 用户   | 简历详情        |
| /jobs                   | JobList.vue              | 用户   | 岗位描述列表    |
| /jobs/create            | JobCreate.vue            | 用户   | 新建岗位描述    |
| /jobs/:id               | JobDetail.vue            | 用户   | 岗位描述详情    |
| /analysis/create        | AnalysisCreate.vue       | 用户   | 创建分析任务    |
| /analysis/tasks/:id     | AnalysisTaskDetail.vue   | 用户   | 分析任务详情    |
| /reports                | ReportList.vue           | 用户   | 分析报告列表    |
| /reports/:id            | ReportDetail.vue         | 用户   | 分析报告详情    |
| /profile                | Profile.vue              | 用户   | 个人中心        |
| /admin                  | AdminDashboard.vue       | 管理员 | 管理后台首页    |
| /admin/users            | UserManage.vue           | 管理员 | 用户管理        |
| /admin/resumes          | ResumeManage.vue         | 管理员 | 简历管理        |
| /admin/analysis-tasks   | AnalysisTaskManage.vue   | 管理员 | 分析任务管理    |
| /admin/api-logs         | ApiLogManage.vue         | 管理员 | API 日志        |
| /admin/prompt-templates | PromptTemplateManage.vue | 管理员 | Prompt 模板管理 |

## 4.3 路由守卫

前端需要实现路由守卫。

规则：

1. 未登录用户访问用户路由时，跳转到 `/login`。
2. 已登录用户访问 `/login` 或 `/register` 时，跳转到 `/dashboard`。
3. 普通用户访问 `/admin` 路由时，跳转到 `/dashboard` 或显示无权限页面。
4. Token 过期时，清除本地登录状态并跳转登录页。

伪代码：

```ts
router.beforeEach((to, from, next) => {
  const authStore = useAuthStore()

  if (to.meta.requiresAuth && !authStore.token) {
    next('/login')
    return
  }

  if (to.meta.requiresAdmin && authStore.user?.role !== 'admin') {
    next('/dashboard')
    return
  }

  next()
})
```

------

## 5. 页面设计

## 5.1 登录页 Login.vue

### 页面目标

让用户通过邮箱和密码登录系统。

### 页面元素

- 系统名称和简介
- 邮箱输入框
- 密码输入框
- 登录按钮
- 注册入口
- 错误提示区域

### 交互流程

1. 用户输入邮箱和密码。
2. 前端校验邮箱格式和密码非空。
3. 调用 `POST /api/auth/login`。
4. 登录成功后保存 Token 和用户信息。
5. 跳转到工作台页面。
6. 登录失败时展示错误提示。

------

## 5.2 注册页 Register.vue

### 页面目标

让新用户创建账号。

### 页面元素

- 邮箱输入框
- 昵称输入框
- 密码输入框
- 确认密码输入框
- 注册按钮
- 登录入口

### 交互流程

1. 用户填写注册信息。
2. 前端校验邮箱格式、密码长度和两次密码一致。
3. 调用 `POST /api/auth/register`。
4. 注册成功后保存 Token 或跳转登录页。
5. 注册失败时提示错误信息。

------

## 5.3 用户工作台 Dashboard.vue

### 页面目标

展示用户当前求职分析概况，并提供主要功能入口。

### 页面元素

- 欢迎语
- 简历数量卡片
- 岗位描述数量卡片
- 分析报告数量卡片
- 最近一次分析结果
- 快捷入口按钮
  - 上传简历
  - 新建岗位
  - 开始分析
  - 查看报告

### 数据接口

```text
GET /api/dashboard/summary
```

### 交互说明

1. 页面加载时请求工作台统计数据。
2. 点击上传简历跳转 `/resumes/upload`。
3. 点击开始分析跳转 `/analysis/create`。
4. 点击最近报告跳转报告详情页。

------

## 5.4 简历列表页 ResumeList.vue

### 页面目标

展示用户已上传的简历，并提供查看、上传、删除等操作。

### 页面元素

- 简历列表表格或卡片
- 上传简历按钮
- 搜索框
- 状态筛选
- 分页器
- 删除确认弹窗

### 列表字段

| 字段     | 说明                 |
| -------- | -------------------- |
| 简历名称 | title                |
| 文件名   | file_name            |
| 文件类型 | file_type            |
| 文件大小 | file_size            |
| 解析状态 | parse_status         |
| 版本号   | version              |
| 是否默认 | is_default           |
| 上传时间 | created_at           |
| 操作     | 查看、设为默认、删除 |

### 数据接口

```text
GET /api/resumes
DELETE /api/resumes/{resume_id}
PATCH /api/resumes/{resume_id}
```

------

## 5.5 简历上传页 ResumeUploadPage.vue

### 页面目标

让用户上传 PDF、DOCX 或 TXT 简历文件。

### 页面元素

- 文件上传组件
- 简历名称输入框
- 文件格式说明
- 文件大小限制说明
- 上传按钮
- 上传进度提示
- 解析结果提示

### 交互流程

1. 用户选择文件。
2. 前端校验文件类型和大小。
3. 用户填写简历名称。
4. 调用 `POST /api/resumes/upload`。
5. 上传成功后展示解析状态。
6. 如果解析成功，提示用户可开始分析。
7. 如果解析失败，提示用户重新上传或后续手动补充文本。

### 前端校验规则

| 校验项   | 规则                       |
| -------- | -------------------------- |
| 文件类型 | pdf/docx/txt               |
| 文件大小 | 不超过后端配置，默认 10 MB |
| 简历名称 | 可选，不填则使用文件名     |

------

## 5.6 简历详情页 ResumeDetail.vue

### 页面目标

展示简历基本信息、解析文本和结构化内容。

### 页面元素

- 简历基本信息卡片
- 原始文本预览区域
- 结构化内容展示区域
- 开始分析按钮
- 重新解析按钮
- 删除按钮

### 数据接口

```text
GET /api/resumes/{resume_id}
POST /api/resumes/{resume_id}/parse
```

### 交互说明

1. 页面加载时获取简历详情。
2. 点击开始分析时跳转到 `/analysis/create`，并带上 resume_id。
3. 点击重新解析时调用解析接口并刷新页面。

------

## 5.7 岗位描述列表页 JobList.vue

### 页面目标

展示用户创建过的目标岗位描述。

### 页面元素

- 岗位列表表格或卡片
- 新建岗位按钮
- 搜索框
- 分页器
- 删除确认弹窗

### 列表字段

| 字段     | 说明             |
| -------- | ---------------- |
| 岗位名称 | title            |
| 公司名称 | company_name     |
| 行业     | industry         |
| 地点     | location         |
| 解析状态 | parse_status     |
| 创建时间 | created_at       |
| 操作     | 查看、编辑、删除 |

### 数据接口

```text
GET /api/jobs
DELETE /api/jobs/{job_id}
```

------

## 5.8 新建岗位页 JobCreate.vue

### 页面目标

让用户录入目标岗位描述。

### 页面元素

- 岗位名称输入框
- 公司名称输入框
- 行业输入框
- 工作地点输入框
- 薪资范围输入框
- 岗位描述文本框
- 保存按钮

### 交互流程

1. 用户填写岗位信息。
2. 前端校验岗位名称和岗位描述非空。
3. 调用 `POST /api/jobs`。
4. 创建成功后跳转岗位详情页。

------

## 5.9 岗位详情页 JobDetail.vue

### 页面目标

展示岗位原文、结构化岗位要求和关键词。

### 页面元素

- 岗位基本信息
- 岗位描述原文
- 必备技能标签
- 加分技能标签
- 岗位关键词标签
- 编辑按钮
- 开始分析按钮
- 重新解析按钮

### 数据接口

```text
GET /api/jobs/{job_id}
PUT /api/jobs/{job_id}
POST /api/jobs/{job_id}/parse
```

------

## 5.10 创建分析任务页 AnalysisCreate.vue

### 页面目标

让用户选择简历和岗位描述，发起 AI 分析。

### 页面元素

- 简历选择框
- 岗位描述选择框
- 分析类型选择
- 简历预览摘要
- 岗位预览摘要
- 开始分析按钮
- 分析说明和隐私提示

### 分析类型

| 类型          | 说明         |
| ------------- | ------------ |
| resume_score  | 仅简历评分   |
| job_match     | 岗位匹配分析 |
| full_analysis | 完整分析报告 |

### 交互流程

1. 页面加载时获取简历列表和岗位列表。
2. 用户选择简历和岗位。
3. 用户选择分析类型。
4. 点击开始分析。
5. 调用 `POST /api/analysis/tasks`。
6. 如果后端同步返回报告 ID，跳转报告详情页。
7. 如果后端返回任务 ID，跳转任务详情页并轮询状态。

### 数据接口

```text
GET /api/resumes
GET /api/jobs
POST /api/analysis/tasks
```

------

## 5.11 分析任务详情页 AnalysisTaskDetail.vue

### 页面目标

展示分析任务执行状态。

### 页面元素

- 任务状态卡片
- 进度条
- 当前状态说明
- 错误信息提示
- 查看报告按钮
- 返回按钮

### 交互流程

1. 页面加载时调用任务详情接口。
2. 如果状态为 pending 或 running，定时轮询。
3. 如果状态为 success，显示查看报告按钮。
4. 如果状态为 failed，显示失败原因和重试入口。

### 数据接口

```text
GET /api/analysis/tasks/{task_id}
```

### 轮询规则

建议每 2 到 3 秒轮询一次。

当任务状态为以下状态时停止轮询：

- success
- failed
- canceled

------

## 5.12 报告列表页 ReportList.vue

### 页面目标

展示用户历史分析报告。

### 页面元素

- 报告列表
- 简历筛选
- 岗位筛选
- 时间排序
- 分页器

### 列表字段

| 字段     | 说明         |
| -------- | ------------ |
| 简历名称 | resume_title |
| 岗位名称 | job_title    |
| 简历总分 | total_score  |
| 匹配度   | match_score  |
| 报告摘要 | summary      |
| 生成时间 | created_at   |
| 操作     | 查看详情     |

### 数据接口

```text
GET /api/reports
```

------

## 5.13 报告详情页 ReportDetail.vue

### 页面目标

展示完整简历分析报告，帮助用户理解简历优势、问题和修改方向。

### 页面结构

1. 报告概览
2. 简历评分
3. 岗位匹配度
4. 优势总结
5. 问题诊断
6. 关键词覆盖分析
7. 修改建议
8. 改写示例
9. 风险提示

### 页面元素

- 总分卡片
- 匹配度卡片
- 雷达图或柱状图
- 关键词标签云
- 优势列表
- 问题列表
- 建议优先级列表
- 改写示例对比卡片
- 返回按钮

### 数据接口

```text
GET /api/reports/{report_id}
```

### 展示建议

#### 总分和匹配度

使用醒目的数字卡片展示：

```text
简历总分：82 / 100
岗位匹配度：76%
```

#### 评分维度

可使用 ECharts 柱状图或雷达图展示：

- 信息完整性
- 结构清晰度
- 表达专业度
- 经历含金量
- 技能匹配度
- 量化成果

#### 修改建议

建议按优先级分组：

- 高优先级
- 中优先级
- 低优先级

#### 改写示例

使用对比卡片展示：

```text
原句
问题说明
改写示例
改写理由
```

------

## 5.14 个人中心页 Profile.vue

### 页面目标

用户维护个人基础信息。

### 页面元素

- 昵称
- 邮箱，只读
- 手机号
- 目标岗位
- 目标城市
- 工作年限
- 保存按钮
- 修改密码入口

### 数据接口

```text
GET /api/auth/me
PUT /api/users/me
PUT /api/users/me/password
```

------

## 6. 管理后台页面设计

## 6.1 管理后台布局

管理后台使用 AdminLayout，包含：

- 顶部导航
- 左侧菜单
- 内容区域
- 管理员信息区域

左侧菜单包括：

1. 后台首页
2. 用户管理
3. 简历管理
4. 分析任务
5. API 日志
6. Prompt 模板

------

## 6.2 管理后台首页 AdminDashboard.vue

### 页面目标

展示系统整体运行数据。

### 页面元素

- 用户总数
- 简历总数
- 岗位描述总数
- 分析任务总数
- 今日 API 调用次数
- 失败任务数

### 数据接口

```text
GET /api/admin/dashboard/summary
```

------

## 6.3 用户管理页 UserManage.vue

### 页面目标

管理员查看和管理用户。

### 页面元素

- 用户表格
- 搜索框
- 状态筛选
- 分页器
- 用户状态操作按钮

### 数据接口

```text
GET /api/admin/users
GET /api/admin/users/{user_id}
PATCH /api/admin/users/{user_id}/status
```

------

## 6.4 简历管理页 ResumeManage.vue

### 页面目标

管理员查看系统内简历上传和解析情况。

### 页面元素

- 简历记录表格
- 用户筛选
- 解析状态筛选
- 查看详情按钮

### 数据接口

```text
GET /api/admin/resumes
GET /api/admin/resumes/{resume_id}
```

### 安全提示

管理员查看用户简历原文属于敏感操作，应在页面上弹出确认提示。

------

## 6.5 分析任务管理页 AnalysisTaskManage.vue

### 页面目标

管理员查看 AI 分析任务执行情况。

### 页面元素

- 任务表格
- 状态筛选
- 任务类型筛选
- 用户筛选
- 失败原因查看

### 数据接口

```text
GET /api/admin/analysis-tasks
```

------

## 6.6 API 日志页 ApiLogManage.vue

### 页面目标

管理员查看 DeepSeek API 调用记录。

### 页面元素

- API 日志表格
- 场景筛选
- 状态筛选
- 时间范围筛选
- 失败原因查看

### 数据接口

```text
GET /api/admin/api-logs
```

------

## 6.7 Prompt 模板管理页 PromptTemplateManage.vue

### 页面目标

管理员维护 AI Prompt 模板。

### 页面元素

- 模板列表
- 新建模板按钮
- 编辑弹窗
- 启用 / 停用按钮
- 模板内容编辑器
- 版本号展示

### 数据接口

```text
GET /api/admin/prompt-templates
POST /api/admin/prompt-templates
PUT /api/admin/prompt-templates/{template_id}
PATCH /api/admin/prompt-templates/{template_id}/status
```

------

## 7. 组件设计

## 7.1 公共组件

| 组件          | 说明       |
| ------------- | ---------- |
| AppHeader     | 顶部导航栏 |
| AppSidebar    | 侧边菜单   |
| AppBreadcrumb | 面包屑导航 |
| PageContainer | 页面容器   |
| EmptyState    | 空数据展示 |
| LoadingState  | 加载中状态 |
| ConfirmDialog | 确认弹窗   |
| StatusTag     | 状态标签   |

## 7.2 简历相关组件

| 组件            | 说明             |
| --------------- | ---------------- |
| ResumeUpload    | 简历上传组件     |
| ResumeCard      | 简历卡片         |
| ResumePreview   | 简历文本预览     |
| ResumeStatusTag | 简历解析状态标签 |

## 7.3 岗位相关组件

| 组件        | 说明       |
| ----------- | ---------- |
| JobForm     | 岗位表单   |
| JobCard     | 岗位卡片   |
| KeywordTags | 关键词标签 |

## 7.4 报告相关组件

| 组件                | 说明           |
| ------------------- | -------------- |
| ScoreCard           | 分数卡片       |
| MatchChart          | 匹配度图表     |
| ScoreBreakdownChart | 评分维度图表   |
| KeywordCoverage     | 关键词覆盖展示 |
| SuggestionList      | 修改建议列表   |
| RewriteExampleList  | 改写示例列表   |
| RiskNoteList        | 风险提示列表   |

------

## 8. 状态管理设计

## 8.1 Pinia Store 划分

建议使用以下 Store：

```text
stores/auth.ts
stores/user.ts
stores/app.ts
```

### authStore

职责：

- 保存 access_token
- 保存当前用户信息
- 登录
- 注册
- 退出登录
- 判断是否管理员

状态示例：

```ts
interface AuthState {
  token: string | null
  user: UserInfo | null
}
```

### userStore

职责：

- 获取用户资料
- 更新用户资料

### appStore

职责：

- 全局加载状态
- 侧边栏折叠状态
- 页面标题

------

## 9. API 请求模块设计

## 9.1 Axios 实例

建议在 `src/api/request.ts` 中创建统一 Axios 实例。

功能：

1. 设置 baseURL。
2. 设置请求超时时间。
3. 请求拦截器自动携带 Token。
4. 响应拦截器统一处理错误。
5. 401 自动跳转登录页。

示例：

```ts
import axios from 'axios'

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 60000
})

request.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

export default request
```

## 9.2 API 文件划分

| 文件         | 说明                       |
| ------------ | -------------------------- |
| auth.ts      | 登录、注册、当前用户       |
| user.ts      | 用户资料、修改密码         |
| resume.ts    | 简历上传、列表、详情、删除 |
| job.ts       | 岗位描述增删改查           |
| analysis.ts  | 分析任务创建和查询         |
| report.ts    | 报告列表和详情             |
| dashboard.ts | 工作台统计                 |
| admin.ts     | 管理后台接口               |

------

## 10. 表单校验设计

## 10.1 登录表单

| 字段     | 规则           |
| -------- | -------------- |
| email    | 必填，邮箱格式 |
| password | 必填           |

## 10.2 注册表单

| 字段             | 规则                 |
| ---------------- | -------------------- |
| email            | 必填，邮箱格式       |
| password         | 必填，不少于 8 位    |
| confirm_password | 必须与 password 一致 |
| nickname         | 可选，长度不超过 100 |

## 10.3 简历上传表单

| 字段  | 规则                      |
| ----- | ------------------------- |
| file  | 必填，只允许 pdf/docx/txt |
| title | 可选，长度不超过 200      |

## 10.4 岗位描述表单

| 字段             | 规则                   |
| ---------------- | ---------------------- |
| title            | 必填，长度不超过 200   |
| description_text | 必填，长度不少于 20 字 |
| company_name     | 可选                   |
| location         | 可选                   |
| salary_range     | 可选                   |

## 10.5 分析任务表单

| 字段               | 规则                                              |
| ------------------ | ------------------------------------------------- |
| resume_id          | 必填                                              |
| task_type          | 必填                                              |
| job_description_id | 当 task_type 为 job_match 或 full_analysis 时必填 |

------

## 11. 交互状态设计

## 11.1 加载状态

需要展示 Loading 的场景：

1. 页面首次加载数据。
2. 文件上传中。
3. 创建分析任务中。
4. 报告详情加载中。
5. 管理后台表格查询中。

## 11.2 空状态

需要展示空状态的场景：

1. 用户暂无简历。
2. 用户暂无岗位描述。
3. 用户暂无分析报告。
4. 管理后台筛选无结果。

空状态应提供下一步操作入口。

示例：

```text
你还没有上传简历，点击按钮上传第一份简历。
```

## 11.3 错误状态

需要展示错误提示的场景：

1. 登录失败。
2. 上传失败。
3. 文件类型不支持。
4. AI 分析失败。
5. 网络异常。
6. 无访问权限。

错误提示应尽量清晰，避免直接展示后端堆栈信息。

------

## 12. 分析报告可视化设计

## 12.1 总分展示

建议使用大号数字卡片展示：

```text
简历总分 82 / 100
```

## 12.2 匹配度展示

建议使用进度环或进度条展示：

```text
岗位匹配度 76%
```

## 12.3 评分维度展示

建议使用柱状图或雷达图展示各维度得分。

维度包括：

- 信息完整性
- 结构清晰度
- 表达专业度
- 经历含金量
- 技能匹配度
- 量化成果

## 12.4 关键词覆盖展示

建议分为两组标签：

1. 已覆盖关键词。
2. 未覆盖关键词。

已覆盖关键词使用积极样式，未覆盖关键词使用警示样式。

## 12.5 修改建议展示

建议按优先级排序。

展示字段：

- 优先级
- 所属模块
- 问题说明
- 修改建议

## 12.6 改写示例展示

建议使用左右或上下对比：

```text
原句：负责后台接口开发
问题：缺少技术细节和结果
改写：基于 FastAPI 设计并实现用户认证、简历上传和分析任务接口
```

------

## 13. 权限与安全设计

## 13.1 Token 存储

一期建议使用 localStorage 存储 Token，开发简单。

后续如提升安全性，可改为 httpOnly Cookie。

## 13.2 页面权限

1. 未登录用户不能访问用户页面。
2. 普通用户不能访问管理员页面。
3. 前端权限控制只用于用户体验，真正权限必须由后端校验。

## 13.3 敏感信息展示

前端不应展示：

- password_hash
- API Key
- 服务器绝对文件路径
- 完整错误堆栈

## 13.4 管理员敏感操作提醒

管理员查看用户简历原文、禁用用户、修改 Prompt 模板时，应弹出确认提示。

------

## 14. 环境变量设计

前端环境变量文件：

```text
.env.development
.env.production
.env.example
```

### .env.example

```text
VITE_APP_TITLE=简历分析系统
VITE_API_BASE_URL=http://localhost:8000/api
```

### 开发环境

```text
VITE_API_BASE_URL=http://localhost:8000/api
```

### 生产环境

```text
VITE_API_BASE_URL=/api
```

------

## 15. Docker 部署设计

## 15.1 开发环境

开发环境可使用 Vite Dev Server：

```text
frontend 容器运行 npm run dev
```

访问地址：

```text
http://localhost:5173
```

## 15.2 生产环境

生产环境构建静态文件：

```bash
npm run build
```

构建结果：

```text
frontend/dist
```

生产环境由 Nginx 托管 `dist` 文件。

## 15.3 前端 Dockerfile 建议

```dockerfile
FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .
RUN npm run build

FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

说明：

如果项目统一使用 deploy/nginx 管理 Nginx，也可以不单独为 frontend 镜像内置 Nginx，而是在统一 Nginx 容器中挂载前端 dist 文件。

------

## 16. Nginx 前端路由配置

由于 Vue 使用前端路由，生产环境刷新页面时需要 fallback 到 `index.html`。

Nginx 配置示例：

```nginx
location / {
    root /usr/share/nginx/html;
    index index.html;
    try_files $uri $uri/ /index.html;
}

location /api/ {
    proxy_pass http://backend:8000/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

------

## 17. MVP 前端开发范围

一期 MVP 必须完成以下页面：

### 公开页面

1. 登录页
2. 注册页

### 用户端页面

1. 工作台
2. 简历列表
3. 简历上传
4. 简历详情
5. 岗位列表
6. 新建岗位
7. 岗位详情
8. 创建分析任务
9. 分析任务状态页
10. 报告列表
11. 报告详情
12. 个人中心

### 管理后台页面

1. 后台首页
2. 用户管理
3. 分析任务管理
4. API 日志
5. Prompt 模板管理

可暂缓页面：

1. 在线简历编辑器
2. 报告 PDF 导出页
3. 简历版本对比页
4. 企业端页面
5. 付费会员页面

------

## 18. 前端测试建议

### 18.1 手动测试重点

1. 登录注册流程。
2. Token 过期跳转。
3. 简历上传校验。
4. 岗位描述创建。
5. 分析任务创建和状态轮询。
6. 报告详情展示。
7. 普通用户访问管理员页面拦截。
8. 管理员后台列表查询。

### 18.2 自动化测试方向，可选

后续可增加：

- Vitest 单元测试
- Vue Test Utils 组件测试
- Playwright 端到端测试

------

## 19. 用户操作主流程

## 19.1 首次使用流程

```text
注册账号
  ↓
登录系统
  ↓
进入工作台
  ↓
上传简历
  ↓
创建岗位描述
  ↓
开始分析
  ↓
查看分析报告
  ↓
根据建议修改简历
```

## 19.2 老用户使用流程

```text
登录系统
  ↓
进入工作台
  ↓
选择已有简历和岗位
  ↓
发起分析
  ↓
查看新报告
  ↓
对比历史报告
```

## 19.3 管理员使用流程

```text
管理员登录
  ↓
进入后台首页
  ↓
查看系统统计
  ↓
查看分析任务和 API 日志
  ↓
维护 Prompt 模板
  ↓
处理异常用户或任务
```

------

## 20. 总结

本前端页面与交互设计文档基于 Vue 3 + Vite + TypeScript + Element Plus 技术栈，围绕求职者的核心使用路径设计页面和交互流程。

一期前端开发应优先完成从注册登录、简历上传、岗位录入、发起分析到报告展示的完整闭环，同时实现基础管理后台。后续可逐步增加在线编辑、PDF 导出、移动端适配、企业端和更丰富的数据可视化能力。