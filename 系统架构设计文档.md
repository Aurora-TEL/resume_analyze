# 简历分析系统系统架构设计文档

## 1. 文档说明

### 1.1 文档目的

本文档用于描述简历分析系统的整体技术架构、模块划分、服务部署方式、数据流转方式、核心技术选型和后续扩展方案。

本文档面向对象包括：

- 项目开发者
- 后端开发人员
- 前端开发人员
- 数据库设计人员
- 部署运维人员
- 项目评审人员

### 1.2 项目概述

简历分析系统是一套面向求职者的智能化简历分析平台。系统支持用户上传简历、录入目标岗位描述，并基于 DeepSeek API 对简历内容进行结构化解析、岗位匹配分析、简历评分、关键词覆盖分析和优化建议生成。

系统后端采用 Python 技术栈，数据库采用 PostgreSQL，部署环境采用 Docker 和 Docker Compose 统一管理，后续可发布到个人云服务器。

------

## 2. 架构设计目标

系统架构设计需要满足以下目标：

1. **易开发**：项目结构清晰，模块边界明确，便于个人或小团队快速开发。
2. **易部署**：通过 Docker 管理环境，减少服务器部署复杂度。
3. **易扩展**：AI 模型调用、文件存储、异步任务、前端形态均可扩展。
4. **可维护**：代码分层清晰，业务逻辑、数据库访问和 AI 调用解耦。
5. **安全可靠**：保护用户简历数据，避免 API Key、密码等敏感信息泄露。
6. **适合云服务器发布**：支持 Nginx 反向代理、HTTPS、数据持久化和备份。

------

## 3. 总体架构

## 3.1 架构概览

系统在开发阶段直接采用前后端分离架构。后端使用 Python FastAPI 提供 RESTful API 服务，前端使用独立 Web 前端项目负责页面展示、交互和接口调用。前后端通过 HTTP API 通信，生产环境中由 Nginx 统一对外提供访问入口，并分别代理前端静态资源和后端 API。

整体架构分为以下层次：

1. 用户访问层
2. 前端应用层与后端 API 层
3. 业务服务层
4. AI 能力层
5. 数据存储层
6. 异步任务层
7. 部署运维层

### 3.2 架构示意

```text
用户浏览器
   │
   │ HTTPS
   ▼
Nginx 反向代理
   │
   │ 内部网络
   ▼
后端 API 服务容器 FastAPI
   │
   ├── 用户认证模块
   ├── 简历管理模块
   ├── 岗位描述模块
   ├── 分析任务模块
   ├── 报告生成模块
   ├── 管理后台模块
   │
   ├── PostgreSQL 数据库
   ├── Redis 缓存 / 任务队列，可选
   ├── Worker 异步任务容器，可选
   ├── 本地文件存储 / 对象存储
   │
   └── DeepSeek API
```

### 3.3 一期推荐架构

一期 MVP 推荐直接使用前后端分离架构：

```text
Nginx
  ├── Frontend 前端应用
  └── FastAPI Backend 后端 API
          ↓
      PostgreSQL
          ↓
      DeepSeek API
```

一期可先不引入 Celery Worker，AI 分析任务可以通过同步接口或轻量后台任务实现。

当分析耗时较长、并发用户增加后，再升级为：

```text
Nginx
  ├── Frontend 前端应用
  └── FastAPI Backend 后端 API
          ↓
      Redis Queue
  ↓
Worker
  ↓
PostgreSQL + DeepSeek API
```

------

## 4. 技术选型

## 4.1 后端技术选型

| 技术                | 用途       | 说明                                        |
| ------------------- | ---------- | ------------------------------------------- |
| Python              | 主开发语言 | 生态成熟，适合 Web、AI、文件解析            |
| FastAPI             | Web 框架   | 性能较好，天然支持 API 文档和 Pydantic 校验 |
| Uvicorn             | ASGI 服务  | 运行 FastAPI 应用                           |
| SQLAlchemy          | ORM        | 管理数据库模型和查询                        |
| Alembic             | 数据库迁移 | 管理数据库表结构版本                        |
| Pydantic            | 数据校验   | 定义请求、响应、配置模型                    |
| Passlib / bcrypt    | 密码加密   | 保护用户密码                                |
| PyJWT / python-jose | JWT 鉴权   | 实现登录态和接口鉴权                        |

## 4.2 前端技术选型

开发阶段直接采用前后端分离方案，前端作为独立项目维护。

推荐前端技术栈：

| 技术         | 用途      | 说明                                       |
| ------------ | --------- | ------------------------------------------ |
| Vue 3        | 前端框架  | 适合中小型后台和业务系统，学习曲线相对平滑 |
| Vite         | 构建工具  | 启动快，配置简单                           |
| TypeScript   | 类型约束  | 提高前端代码可维护性                       |
| Pinia        | 状态管理  | 管理用户登录状态、全局数据等               |
| Vue Router   | 路由管理  | 管理用户端和管理端页面路由                 |
| Axios        | HTTP 请求 | 调用 FastAPI 后端接口                      |
| Element Plus | UI 组件库 | 快速构建表单、表格、弹窗、后台页面         |
| ECharts      | 图表展示  | 展示评分、匹配度、关键词覆盖等可视化内容   |

### 推荐结论

本项目前端建议采用 **Vue 3 + Vite + TypeScript + Element Plus**。

选择原因：

1. 适合开发用户端和管理后台。
2. 表单、表格、上传组件、进度条、评分展示等能力完善。
3. 与 FastAPI RESTful API 配合简单。
4. 便于后续扩展为更完整的产品。
5. 部署时可以构建为静态文件，由 Nginx 托管。

### 前后端分离职责边界

前端负责：

- 页面展示
- 表单交互
- 文件上传交互
- Token 本地保存和请求携带
- 调用后端 API
- 分析报告可视化展示
- 管理后台界面

后端负责：

- 用户认证和权限校验
- 业务接口提供
- 文件保存和解析
- PostgreSQL 数据读写
- DeepSeek API 调用
- 分析任务调度
- 报告生成和存储
- 系统日志和安全控制

## 4.3 数据库技术选型

数据库采用 PostgreSQL。

选择原因：

1. 支持 JSONB，适合存储 AI 分析结果。
2. 支持复杂查询和索引。
3. 稳定可靠，适合生产环境。
4. 与 SQLAlchemy 配合成熟。
5. 适合 Docker 部署。

## 4.4 缓存和任务队列

Redis 作为可选组件。

用途：

1. 存储异步任务队列。
2. 缓存分析任务状态。
3. 缓存短期验证码或登录限制信息。
4. 后续支持限流。

一期 MVP 如果功能简单，可暂不引入 Redis。若 AI 分析耗时明显，则建议引入 Redis + Celery / RQ。

## 4.5 AI 模型服务

系统通过后端统一调用 DeepSeek API。

设计原则：

1. 前端不得直接调用 DeepSeek API。
2. API Key 只保存在服务器环境变量中。
3. 后端统一封装模型调用逻辑。
4. Prompt 模板与业务代码解耦。
5. AI 返回结果需要做格式校验。
6. 后续可扩展支持其他模型供应商。

## 4.6 部署技术选型

| 技术              | 用途                               |
| ----------------- | ---------------------------------- |
| Docker            | 容器化应用环境                     |
| Docker Compose    | 编排多个容器服务                   |
| Nginx             | 反向代理、静态资源服务、HTTPS 入口 |
| Ubuntu LTS        | 云服务器操作系统                   |
| Certbot / acme.sh | HTTPS 证书申请，可选               |
| Git               | 代码拉取与发布                     |

------

## 5. 系统模块划分

## 5.1 用户认证模块

职责：

- 用户注册
- 用户登录
- JWT 生成与校验
- 密码加密与验证
- 用户角色判断
- 登录状态管理

核心文件建议：

```text
app/api/auth.py
app/services/auth_service.py
app/models/user.py
app/schemas/auth_schema.py
app/core/security.py
```

## 5.2 用户中心模块

职责：

- 用户资料维护
- 求职方向维护
- 账号状态展示

核心文件建议：

```text
app/api/users.py
app/services/user_service.py
app/schemas/user_schema.py
```

## 5.3 简历管理模块

职责：

- 简历上传
- 文件类型校验
- 文件保存
- 简历文本解析
- 简历结构化存储
- 简历列表和详情查询
- 简历删除

核心文件建议：

```text
app/api/resumes.py
app/services/resume_service.py
app/services/file_service.py
app/services/parser_service.py
app/models/resume.py
app/schemas/resume_schema.py
```

## 5.4 岗位描述模块

职责：

- 新建岗位描述
- 岗位描述解析
- 岗位关键词提取
- 岗位列表查询
- 岗位详情查询
- 岗位删除

核心文件建议：

```text
app/api/jobs.py
app/services/job_service.py
app/models/job_description.py
app/schemas/job_schema.py
```

## 5.5 AI 分析模块

职责：

- 构建 Prompt
- 调用 DeepSeek API
- 解析 AI 返回内容
- 校验分析结果
- 生成简历评分
- 生成岗位匹配分析
- 生成优化建议

核心文件建议：

```text
app/services/ai/deepseek_client.py
app/services/ai/prompt_builder.py
app/services/ai/response_parser.py
app/services/analysis_service.py
app/models/prompt_template.py
```

## 5.6 分析任务模块

职责：

- 创建分析任务
- 管理任务状态
- 执行分析流程
- 保存分析结果
- 查询任务状态

核心文件建议：

```text
app/api/analysis.py
app/services/task_service.py
app/models/analysis_task.py
app/models/analysis_report.py
app/schemas/analysis_schema.py
```

## 5.7 报告模块

职责：

- 分析报告展示
- 报告摘要生成
- 历史报告查询
- 报告详情查询
- 后续支持报告导出

核心文件建议：

```text
app/api/reports.py
app/services/report_service.py
app/templates/reports/detail.html
app/templates/reports/list.html
```

## 5.8 管理后台模块

职责：

- 用户管理
- 简历记录管理
- 分析任务管理
- API 调用日志管理
- Prompt 模板管理
- 系统统计

核心文件建议：

```text
app/api/admin.py
app/services/admin_service.py
app/templates/admin/
```

## 5.9 日志与监控模块

职责：

- 记录系统日志
- 记录 DeepSeek API 调用日志
- 记录异常信息
- 记录管理员操作日志

核心文件建议：

```text
app/core/logging.py
app/models/api_call_log.py
app/models/admin_operation_log.py
```

------

## 6. 项目目录结构设计

采用前后端分离后，项目建议使用单仓库 Monorepo 结构，前端、后端、部署配置分别管理。

推荐目录结构如下：

```text
resume-analyzer/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── api/
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── resumes.py
│   │   │   ├── jobs.py
│   │   │   ├── analysis.py
│   │   │   ├── reports.py
│   │   │   └── admin.py
│   │   ├── core/
│   │   │   ├── config.py
│   │   │   ├── database.py
│   │   │   ├── security.py
│   │   │   ├── logging.py
│   │   │   └── exceptions.py
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── services/
│   │   │   └── ai/
│   │   └── tasks/
│   ├── migrations/
│   ├── tests/
│   ├── Dockerfile
│   ├── requirements.txt
│   └── alembic.ini
├── frontend/
│   ├── src/
│   │   ├── api/
│   │   ├── assets/
│   │   ├── components/
│   │   ├── router/
│   │   ├── stores/
│   │   ├── views/
│   │   │   ├── auth/
│   │   │   ├── dashboard/
│   │   │   ├── resumes/
│   │   │   ├── jobs/
│   │   │   ├── reports/
│   │   │   └── admin/
│   │   ├── App.vue
│   │   └── main.ts
│   ├── public/
│   ├── Dockerfile
│   ├── package.json
│   ├── vite.config.ts
│   └── tsconfig.json
├── deploy/
│   ├── nginx/
│   │   └── nginx.conf
│   ├── scripts/
│   │   ├── backup_db.sh
│   │   ├── restore_db.sh
│   │   └── init_server.sh
│   └── docker/
├── storage/
│   └── resumes/
├── docker-compose.yml
├── docker-compose.prod.yml
├── .env.example
├── .gitignore
└── README.md
```

目录说明：

- `backend`：FastAPI 后端项目。
- `frontend`：Vue 3 前端项目。
- `deploy`：Nginx、备份脚本和服务器部署相关配置。
- `storage`：本地开发和生产挂载的上传文件目录。
- `docker-compose.yml`：开发环境编排文件。
- `docker-compose.prod.yml`：生产环境编排文件。

------

## 7. 数据流设计

## 7.1 用户上传简历数据流

```text
用户选择简历文件
  ↓
Vue 前端通过 Axios 提交上传请求
  ↓
后端校验用户身份
  ↓
后端校验文件格式和大小
  ↓
保存原始文件到上传目录
  ↓
解析文件文本内容
  ↓
保存简历记录到 PostgreSQL
  ↓
返回上传结果
```

关键点：

1. 上传文件必须校验格式。
2. 文件路径保存到数据库。
3. 原始文本保存到数据库，便于后续 AI 分析。
4. 原始文件保存到持久化目录。

## 7.2 岗位描述创建数据流

```text
用户输入岗位描述
  ↓
后端校验用户身份
  ↓
保存岗位原文
  ↓
可选：调用 AI 或规则解析岗位关键词
  ↓
保存结构化岗位信息
  ↓
返回岗位记录
```

## 7.3 简历分析数据流

```text
用户选择简历和岗位
  ↓
后端校验资源归属
  ↓
创建分析任务
  ↓
读取简历文本和岗位描述
  ↓
构建 Prompt
  ↓
调用 DeepSeek API
  ↓
解析 AI 返回结果
  ↓
校验 JSON 格式和字段完整性
  ↓
保存分析报告
  ↓
更新任务状态
  ↓
Vue 前端调用报告接口并展示分析报告
```

## 7.4 异步分析数据流，可选

```text
用户发起分析
  ↓
后端创建 pending 任务
  ↓
任务写入 Redis 队列
  ↓
Worker 拉取任务
  ↓
Worker 调用 DeepSeek API
  ↓
Worker 保存报告结果
  ↓
Vue 前端轮询任务状态接口
  ↓
任务完成后展示报告
```

适用场景：

- 简历较长
- AI 分析耗时较高
- 用户并发增加
- 需要避免 HTTP 请求超时

------

## 8. 数据库架构设计

数据库采用 PostgreSQL。

核心数据表包括：

1. users：用户表
2. resumes：简历表
3. job_descriptions：岗位描述表
4. analysis_tasks：分析任务表
5. analysis_reports：分析报告表
6. api_call_logs：API 调用日志表
7. prompt_templates：Prompt 模板表
8. admin_operation_logs：管理员操作日志表，可选

## 8.1 数据关系

```text
users 1 ─── N resumes
users 1 ─── N job_descriptions
users 1 ─── N analysis_tasks
users 1 ─── N analysis_reports

resumes 1 ─── N analysis_tasks
job_descriptions 1 ─── N analysis_tasks
analysis_tasks 1 ─── 1 analysis_reports
analysis_tasks 1 ─── N api_call_logs
```

## 8.2 JSONB 使用建议

以下字段建议使用 JSONB：

| 表               | 字段            | 用途                       |
| ---------------- | --------------- | -------------------------- |
| resumes          | structured_data | 存储结构化简历内容         |
| job_descriptions | structured_data | 存储岗位关键词、技能要求等 |
| analysis_reports | report_data     | 存储完整分析报告           |

使用 JSONB 的原因：

1. AI 返回结果结构可能随版本变化。
2. 报告内容字段较多，使用 JSONB 更灵活。
3. PostgreSQL 对 JSONB 支持较好，可建立索引。

------

## 9. AI 服务架构设计

## 9.1 AI 调用分层

AI 服务建议拆分为三层：

```text
analysis_service.py
  ↓
prompt_builder.py
  ↓
deepseek_client.py
  ↓
DeepSeek API
```

职责说明：

| 层级             | 职责                   |
| ---------------- | ---------------------- |
| analysis_service | 编排完整分析流程       |
| prompt_builder   | 根据场景构建 Prompt    |
| deepseek_client  | 封装 DeepSeek API 请求 |
| response_parser  | 解析和校验 AI 返回结果 |

## 9.2 AI 分析场景

系统至少包含以下 AI 场景：

1. 简历结构化解析
2. 岗位描述结构化解析
3. 简历基础评分
4. 简历与岗位匹配分析
5. 关键词覆盖分析
6. 简历优化建议生成
7. 简历句子改写示例生成

## 9.3 Prompt 模板管理

Prompt 模板应支持数据库存储，方便后续调整。

字段包括：

- 模板名称
- 使用场景
- 模板内容
- 版本号
- 是否启用
- 创建时间
- 更新时间

## 9.4 AI 输出格式要求

AI 返回结果应优先要求 JSON 格式。

通用要求：

1. 必须返回合法 JSON。
2. 分数字段必须为数字。
3. 列表字段必须为数组。
4. 不确定信息应返回 null 或“不确定”。
5. 不允许虚构用户经历。
6. 不允许建议用户编造项目或技能。

## 9.5 DeepSeek API 异常处理

异常类型包括：

| 异常         | 处理方式                     |
| ------------ | ---------------------------- |
| 请求超时     | 记录日志，可重试一次         |
| API Key 错误 | 返回系统配置错误，通知管理员 |
| 限流         | 提示用户稍后重试             |
| 返回格式错误 | 尝试修复 JSON 或重新请求     |
| 输入过长     | 截断、摘要或分段处理         |
| 网络异常     | 返回友好错误提示并记录日志   |

------

## 10. 部署架构设计

## 10.1 Docker Compose 服务

生产环境推荐服务：

```text
services:
  nginx:
    对外暴露 80 / 443，代理前端页面和后端 API

  frontend:
    构建 Vue 前端静态资源，或通过 Nginx 托管 dist 文件

  backend:
    运行 FastAPI 后端 API 服务

  db:
    运行 PostgreSQL

  redis:
    运行 Redis，可选

  worker:
    运行异步任务，可选
```

开发环境推荐服务：

```text
services:
  frontend:
    运行 Vite 开发服务器

  backend:
    运行 FastAPI 开发服务器

  db:
    运行 PostgreSQL

  redis:
    运行 Redis，可选
```

开发环境访问方式：

- 前端：`http://localhost:5173`
- 后端 API：`http://localhost:8000`
- API 文档：`http://localhost:8000/docs`

生产环境访问方式：

- 前端页面：`https://your-domain.com`
- 后端 API：`https://your-domain.com/api`

## 10.2 生产环境网络设计

```text
公网
 ↓
Nginx 容器 80 / 443
 ├── 前端静态资源
 └── /api 反向代理到 Backend 容器
       ↓
   Docker 内部网络
       ↓
   PostgreSQL / Redis
```

安全要求：

1. 只暴露 Nginx 的 80 和 443 端口。
2. Frontend 和 Backend 容器端口不直接暴露公网。
3. PostgreSQL 不直接暴露公网。
4. Redis 不直接暴露公网。
5. 所有敏感配置通过环境变量注入。

## 10.3 数据持久化

需要持久化的数据：

1. PostgreSQL 数据目录
2. 用户上传的简历文件
3. 数据库备份文件
4. 应用日志，可选
5. Nginx 日志，可选

推荐宿主机目录：

```text
/opt/resume-analyzer/postgres_data
/opt/resume-analyzer/uploads
/opt/resume-analyzer/backups
/opt/resume-analyzer/logs
```

## 10.4 云服务器部署建议

个人云服务器推荐配置：

| 资源 | 建议                 |
| ---- | -------------------- |
| CPU  | 2 核及以上           |
| 内存 | 2 GB 起步，推荐 4 GB |
| 磁盘 | 40 GB 起步           |
| 系统 | Ubuntu LTS           |
| 端口 | 22、80、443          |
| 域名 | 推荐绑定域名         |

## 10.5 发布流程

```text
本地开发
  ↓
提交代码到 Git 仓库
  ↓
服务器拉取最新代码
  ↓
构建 Docker 镜像
  ↓
启动 / 重启容器
  ↓
执行数据库迁移
  ↓
检查服务状态
  ↓
完成发布
```

------

## 11. 安全架构设计

## 11.1 用户认证安全

1. 密码使用哈希算法加密存储。
2. 登录成功后签发 JWT。
3. JWT 设置过期时间。
4. 管理员接口必须校验角色。
5. 连续登录失败可限制请求频率。

## 11.2 数据访问安全

1. 普通用户只能访问自己的简历、岗位和报告。
2. 后端每次查询用户资源时必须校验 user_id。
3. 管理员访问用户简历原文需要记录日志。
4. 删除操作建议使用软删除。

## 11.3 文件上传安全

1. 限制文件类型。
2. 限制文件大小。
3. 上传文件重命名，避免路径注入。
4. 文件保存路径不直接暴露。
5. 禁止执行上传目录中的文件。

## 11.4 API Key 安全

1. DeepSeek API Key 只能配置在服务器环境变量中。
2. 不得提交到 Git 仓库。
3. 不得返回给前端。
4. 日志中不得输出 API Key。

## 11.5 日志安全

日志中不得记录以下内容：

- 用户密码
- JWT Token
- DeepSeek API Key
- 完整简历原文
- 完整手机号
- 完整邮箱，可按需脱敏

------

## 12. 可扩展架构设计

## 12.1 多模型扩展

AI 服务层应抽象为统一接口。

后续可支持：

- DeepSeek
- OpenAI
- Claude
- Qwen
- 本地大模型

建议接口：

```python
class BaseLLMClient:
    def chat(self, messages: list[dict], model: str) -> str:
        pass
```

## 12.2 文件存储扩展

一期使用本地文件存储。

后续可扩展为：

- MinIO
- 阿里云 OSS
- 腾讯云 COS
- AWS S3

## 12.3 前端扩展

项目从开发初期即采用前后端分离架构。

一期前端建议使用：

```text
Vue 3 + Vite + TypeScript + Element Plus
```

后续前端可扩展：

1. 增加更完整的用户工作台。
2. 增加可视化分析图表。
3. 增加在线简历编辑器。
4. 增加报告 PDF 导出页面。
5. 增加移动端适配。
6. 增加企业端或管理员独立后台。

## 12.4 异步任务扩展

一期可同步分析。

后续可使用：

- Celery + Redis
- RQ + Redis
- Dramatiq

## 12.5 报告导出扩展

后续支持：

- 导出 PDF 报告
- 导出 Word 报告
- 导出优化后的简历草稿
- 生成求职信

------

## 13. 核心时序设计

## 13.1 用户登录时序

```text
用户输入邮箱和密码
  ↓
后端查询用户
  ↓
校验密码哈希
  ↓
生成 JWT
  ↓
返回用户信息和 Token
```

## 13.2 简历上传时序

```text
用户上传文件
  ↓
后端校验 Token
  ↓
校验文件格式和大小
  ↓
保存文件
  ↓
解析文本
  ↓
写入 resumes 表
  ↓
返回简历 ID
```

## 13.3 简历分析时序

```text
用户点击开始分析
  ↓
创建 analysis_task
  ↓
读取 resume 和 job_description
  ↓
构建 Prompt
  ↓
调用 DeepSeek API
  ↓
解析 JSON
  ↓
保存 analysis_report
  ↓
更新任务状态为 success
  ↓
展示报告
```

## 13.4 异步任务时序

```text
用户点击开始分析
  ↓
创建 pending 任务
  ↓
返回 task_id
  ↓
Worker 执行任务
  ↓
用户端轮询任务状态
  ↓
任务完成后跳转报告页
```

------

## 14. 环境配置设计

## 14.1 环境划分

系统至少划分为：

| 环境 | 说明           |
| ---- | -------------- |
| dev  | 本地开发环境   |
| test | 测试环境，可选 |
| prod | 生产环境       |

## 14.2 .env.example 示例

```text
APP_ENV=dev
APP_DEBUG=true
APP_SECRET_KEY=change-me

DATABASE_URL=postgresql+psycopg2://resume_user:resume_pass@db:5432/resume_analyzer

REDIS_URL=redis://redis:6379/0

DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

JWT_SECRET_KEY=change-me
JWT_EXPIRE_MINUTES=1440

UPLOAD_DIR=/app/storage/resumes
MAX_UPLOAD_SIZE_MB=10

LOG_LEVEL=INFO
ALLOWED_ORIGINS=*
```

## 14.3 配置读取方式

后端应通过 Pydantic Settings 或类似方式统一读取配置。

建议文件：

```text
app/core/config.py
```

配置类示例：

```python
class Settings(BaseSettings):
    app_env: str
    app_debug: bool
    database_url: str
    deepseek_api_key: str
    upload_dir: str
```

------

## 15. 架构风险与应对

| 风险              | 说明                   | 应对措施                                   |
| ----------------- | ---------------------- | ------------------------------------------ |
| AI 调用耗时较长   | 同步请求可能超时       | 引入异步任务和轮询机制                     |
| 云服务器资源不足  | 个人服务器配置有限     | 控制并发，优化容器资源，占用较高时升级配置 |
| 简历文件解析失败  | PDF 或 DOCX 格式复杂   | 提供文本粘贴兜底方案                       |
| 数据丢失          | 数据库或上传文件未备份 | 配置定时备份和恢复脚本                     |
| API Key 泄露      | 可能造成费用和安全风险 | 使用环境变量，禁止写入代码仓库             |
| Prompt 输出不稳定 | 影响报告质量           | 使用 JSON Schema 校验、重试和模板版本管理  |
| 数据隐私风险      | 简历包含个人敏感信息   | 权限控制、日志脱敏、删除机制和隐私提示     |

------

## 16. MVP 架构建议

一期 MVP 建议采用以下最小架构：

```text
Vue 3 + Vite + TypeScript + Element Plus
FastAPI RESTful API
PostgreSQL
DeepSeek API
Docker Compose
Nginx
```

暂缓引入：

- Celery Worker
- Redis
- 对象存储
- 自动化 CI/CD
- 多模型适配

原因：

1. 降低开发复杂度。
2. 快速实现核心业务闭环。
3. 便于个人云服务器部署。
4. 后续具备明确扩展路径。

------

## 17. 后续文档清单

建议继续编写以下配套文档：

1. 数据库设计文档
2. 接口 API 设计文档
3. Docker 部署文档
4. DeepSeek Prompt 模板设计文档
5. 前端页面原型说明文档
6. 后端开发说明文档
7. 测试计划文档
8. 项目 README 文档

------

## 18. 总结

本系统采用 Vue 前端 + Python FastAPI 后端 + PostgreSQL + DeepSeek API + Docker Compose 的前后端分离架构。该架构适合个人开发、毕业设计、作品集项目和个人云服务器部署。

一期应优先实现核心业务闭环：用户登录、简历上传、岗位描述录入、AI 分析、报告展示和历史记录。架构上保留 Redis、Worker、对象存储和多模型接入的扩展空间，以便后续根据系统规模逐步升级。