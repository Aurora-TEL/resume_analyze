# 简历分析系统项目需求文档

## 1. 项目概述

### 1.1 项目名称

简历分析系统

### 1.2 项目背景

求职者在投递岗位前，通常需要根据岗位描述优化简历内容，但人工修改存在耗时长、缺乏针对性、难以量化评估等问题。招聘侧也需要快速识别简历中的核心能力、项目经验、技能匹配度和潜在风险。

本项目拟开发一套基于 Python 前后端、PostgreSQL 数据库和 DeepSeek API 的简历分析系统，为求职者提供简历解析、岗位匹配、简历评分、优化建议、关键词提取、能力画像等功能，帮助用户更高效地准备求职材料。

### 1.3 项目目标

1. 支持用户上传简历文件，并自动解析简历内容。
2. 支持用户输入或上传目标岗位描述，分析简历与岗位的匹配程度。
3. 调用 DeepSeek API 对简历内容进行结构化分析、评分和优化建议生成。
4. 支持生成可读性强、可执行性高的简历修改建议。
5. 支持用户保存历史分析记录，便于多版本简历对比和持续优化。
6. 支持后台管理用户、简历、分析记录、API 调用日志和系统配置。

### 1.4 项目范围

本项目一期主要面向求职者个人使用，重点解决“简历是否适配目标岗位”“简历如何修改”“简历中有哪些优势与短板”等问题。

一期范围包括：

- 用户注册、登录、个人信息管理
- 简历上传与文本解析
- 岗位描述输入与管理
- 简历内容结构化分析
- 简历与岗位匹配分析
- 简历评分与优化建议
- 分析报告生成与历史记录管理
- 管理后台基础功能
- DeepSeek API 调用封装与日志记录
- PostgreSQL 数据持久化

暂不纳入一期范围：

- 自动投递简历
- 与招聘网站账号打通
- 企业 HR 招聘管理系统
- 多人协作评审
- 在线简历编辑器的复杂排版功能
- 付费订阅和支付系统

------

## 2. 用户角色

### 2.1 求职者用户

主要使用系统进行简历上传、岗位匹配分析、优化建议查看、历史报告管理。

核心诉求：

- 快速判断简历质量
- 明确简历与岗位的匹配度
- 获得具体、可执行的修改建议
- 对比不同版本简历的优化效果

### 2.2 管理员

负责系统基础管理、用户管理、简历记录管理、模型调用监控和系统配置。

核心诉求：

- 查看系统使用情况
- 管理异常用户或异常内容
- 监控 DeepSeek API 调用状态和成本
- 维护提示词模板、评分规则和系统配置

------

## 3. 总体业务流程

### 3.1 求职者使用流程

1. 用户注册或登录系统。
2. 用户上传简历文件，或粘贴简历文本。
3. 系统解析简历内容，提取基本信息、教育经历、工作经历、项目经历、技能、证书等模块。
4. 用户输入目标岗位描述，或从历史岗位描述中选择一个岗位。
5. 用户发起简历分析。
6. 系统调用后端分析服务，将简历内容和岗位描述提交给 DeepSeek API。
7. DeepSeek 返回结构化分析结果。
8. 系统生成分析报告，包括简历评分、岗位匹配度、关键词覆盖情况、优势分析、问题诊断和修改建议。
9. 用户查看报告，并可保存、导出或重新分析。
10. 用户可上传新版本简历，与历史版本对比。

### 3.2 管理员使用流程

1. 管理员登录后台。
2. 查看用户列表、简历上传记录和分析任务记录。
3. 查看 API 调用日志、失败日志、调用耗时和 token 消耗。
4. 管理提示词模板和评分配置。
5. 处理异常数据或用户反馈。

------

## 4. 功能需求

## 4.1 用户认证模块

### 4.1.1 用户注册

功能描述：
用户可以通过邮箱和密码注册账号。

输入字段：

- 邮箱
- 密码
- 确认密码
- 用户昵称，可选

业务规则：

- 邮箱必须唯一。
- 密码需要进行加密存储。
- 密码长度不少于 8 位。
- 注册成功后自动创建用户基础档案。

### 4.1.2 用户登录

功能描述：
用户通过邮箱和密码登录系统。

业务规则：

- 登录成功后返回访问令牌。
- 访问令牌用于后续接口鉴权。
- 连续登录失败次数过多时，应触发短时间限制。

### 4.1.3 用户退出登录

功能描述：
用户可以主动退出当前登录状态。

### 4.1.4 用户个人信息管理

功能描述：
用户可以维护基础信息。

字段包括：

- 昵称
- 邮箱
- 手机号，可选
- 求职方向
- 工作年限
- 目标城市
- 期望岗位

------

## 4.2 简历管理模块

### 4.2.1 简历上传

功能描述：
用户可以上传简历文件。

支持格式：

- PDF
- DOCX
- TXT

上传限制：

- 单个文件大小建议不超过 10 MB。
- 每个用户可保留多份简历。
- 文件上传后需要保存原始文件和解析后的文本内容。

### 4.2.2 简历文本解析

功能描述：
系统将上传的简历文件解析为纯文本，并进一步提取结构化字段。

解析内容包括：

- 姓名
- 联系方式
- 邮箱
- 教育背景
- 工作经历
- 项目经历
- 技能清单
- 证书与奖项
- 自我评价
- 其他补充信息

业务规则：

- 如果自动解析失败，应允许用户手动粘贴文本。
- 解析后的文本应保留原始段落结构。
- 结构化字段可以由规则解析和 AI 解析共同完成。

### 4.2.3 简历列表

功能描述：
用户可以查看自己上传过的简历列表。

列表字段：

- 简历名称
- 上传时间
- 文件类型
- 最近分析时间
- 简历版本号
- 状态

### 4.2.4 简历详情

功能描述：
用户可以查看某份简历的原始文本、结构化内容和历史分析记录。

### 4.2.5 简历删除

功能描述：
用户可以删除自己的简历。

业务规则：

- 删除可采用软删除。
- 删除后普通用户不可见，管理员可在后台查看删除记录。

------

## 4.3 岗位描述管理模块

### 4.3.1 新建岗位描述

功能描述：
用户可以输入目标岗位描述，用于后续匹配分析。

字段包括：

- 岗位名称
- 公司名称，可选
- 行业，可选
- 工作地点，可选
- 岗位描述正文
- 岗位要求
- 薪资范围，可选

### 4.3.2 岗位描述解析

功能描述：
系统对岗位描述进行结构化解析。

解析内容包括：

- 岗位核心职责
- 必备技能
- 加分技能
- 工作年限要求
- 学历要求
- 行业背景要求
- 软技能要求
- 关键词列表

### 4.3.3 岗位描述列表

功能描述：
用户可以查看和复用历史岗位描述。

### 4.3.4 岗位描述删除

功能描述：
用户可以删除不再使用的岗位描述。

------

## 4.4 简历分析模块

### 4.4.1 简历基础评分

功能描述：
系统根据简历完整性、表达质量、结构清晰度、经历质量、技能呈现等维度进行评分。

评分维度建议：

| 维度       | 分值 | 说明                                               |
| ---------- | ---- | -------------------------------------------------- |
| 信息完整性 | 20   | 是否包含教育、经历、项目、技能、联系方式等核心信息 |
| 结构清晰度 | 15   | 模块是否清楚，层级是否合理                         |
| 表达专业度 | 20   | 是否使用专业、具体、结果导向的表达                 |
| 经历含金量 | 20   | 工作与项目经历是否体现能力和成果                   |
| 技能匹配度 | 15   | 技能是否与目标岗位相关                             |
| 量化成果   | 10   | 是否有数据化结果、指标或业务影响                   |

输出内容：

- 总分
- 各维度得分
- 评分理由
- 主要问题
- 优先修改建议

### 4.4.2 岗位匹配分析

功能描述：
系统将简历内容与岗位描述进行匹配，输出匹配度和差距分析。

分析维度：

- 技能匹配
- 工作经验匹配
- 项目经验匹配
- 行业背景匹配
- 关键词覆盖率
- 软技能匹配
- 学历或证书匹配

输出内容：

- 总体匹配度百分比
- 匹配项列表
- 缺失项列表
- 风险项列表
- 可强化内容
- 针对岗位的简历修改建议

### 4.4.3 关键词分析

功能描述：
系统从岗位描述中提取关键词，并检查简历中是否覆盖。

输出内容：

- 岗位关键词列表
- 简历已覆盖关键词
- 简历未覆盖关键词
- 建议补充关键词
- 关键词使用建议

业务规则：

- 关键词建议必须基于用户真实经历，不应鼓励虚假包装。
- 对于缺失但用户可能具备的技能，应提示用户确认后再加入。

### 4.4.4 简历问题诊断

功能描述：
系统识别简历中的常见问题。

问题类型：

- 表达空泛
- 缺少量化结果
- 技能堆砌
- 项目描述不完整
- 与岗位无关内容过多
- 时间线不清晰
- 工作成果不突出
- 缺少动词和结果导向表达
- 重复表述
- 语病或格式不统一

### 4.4.5 优化建议生成

功能描述：
系统基于分析结果生成具体修改建议。

建议类型：

- 总体修改建议
- 分模块修改建议
- 针对岗位的关键词补充建议
- 项目经历改写建议
- 工作经历改写建议
- 技能模块优化建议
- 自我评价优化建议

建议要求：

- 具体明确，避免笼统表达。
- 不得虚构用户经历。
- 应提示用户根据真实经历确认后修改。
- 优先给出影响最大的修改项。

### 4.4.6 简历改写示例

功能描述：
系统可以对用户简历中的部分句子给出改写示例。

输出格式：

- 原句
- 问题说明
- 改写示例
- 改写理由

示例：

原句：负责用户数据分析。

问题：表达过于笼统，缺少工具、方法和结果。

改写示例：使用 Python 和 SQL 对用户行为数据进行清洗与分析，构建留存率和转化率指标看板，支持运营团队优化活动策略。

业务规则：

- 改写不能编造不存在的工具、数据或结果。
- 如原简历缺少必要信息，应以“可补充方向”形式提示用户。

### 4.4.7 分析报告生成

功能描述：
系统生成完整分析报告。

报告结构：

1. 简历总体评分
2. 岗位匹配度
3. 简历优势总结
4. 核心问题诊断
5. 关键词覆盖分析
6. 分模块修改建议
7. 高优先级修改清单
8. 可参考改写示例
9. 风险提示

### 4.4.8 历史分析记录

功能描述：
用户可以查看历史分析结果。

字段包括：

- 分析时间
- 简历名称
- 岗位名称
- 总分
- 匹配度
- 分析状态
- 报告详情

------

## 4.5 AI 能力接入模块

### 4.5.1 DeepSeek API 调用封装

功能描述：
后端系统统一封装 DeepSeek API 调用，前端不直接访问 DeepSeek API。

封装内容：

- API Key 管理
- 请求参数组装
- Prompt 模板管理
- 响应解析
- 错误重试
- 超时控制
- 调用日志记录
- token 或调用成本统计

### 4.5.2 Prompt 模板管理

功能描述：
系统内置不同分析场景的 Prompt 模板。

模板类型：

- 简历结构化解析模板
- 岗位描述解析模板
- 简历评分模板
- 岗位匹配分析模板
- 关键词提取模板
- 简历优化建议模板
- 简历句子改写模板

Prompt 输出要求：

- 优先要求模型返回 JSON 格式。
- 每个字段必须有明确含义。
- 对于无法判断的信息，应返回 null 或“不确定”，不得编造。

### 4.5.3 AI 响应校验

功能描述：
系统需要对 DeepSeek 返回结果进行校验。

校验规则：

- JSON 格式是否正确。
- 必填字段是否存在。
- 分数是否在合法范围内。
- 是否存在明显空结果。
- 是否存在与输入无关的内容。

### 4.5.4 异常处理

异常类型：

- API 超时
- API 限流
- API Key 无效
- 返回格式错误
- 模型输出为空
- 网络错误
- 用户输入过长

处理方式：

- 返回友好错误提示。
- 记录详细错误日志。
- 支持必要情况下重试。
- 对超长文本进行截断、摘要或分段处理。

------

## 4.6 管理后台模块

### 4.6.1 管理员登录

功能描述：
管理员通过后台入口登录。

### 4.6.2 用户管理

功能描述：
管理员查看和管理用户。

功能包括：

- 用户列表
- 用户详情
- 用户状态管理
- 用户简历数量查看
- 用户分析次数查看

### 4.6.3 简历管理

功能描述：
管理员可以查看简历上传记录和解析状态。

注意：
管理员查看简历原文应受到权限控制，并记录访问日志。

### 4.6.4 分析记录管理

功能描述：
管理员可以查看分析任务状态和分析结果摘要。

字段包括：

- 用户
- 简历
- 岗位描述
- 分析时间
- 分析状态
- 总分
- 匹配度
- 错误信息

### 4.6.5 API 调用日志管理

功能描述：
管理员查看 DeepSeek API 调用情况。

字段包括：

- 调用时间
- 调用场景
- 用户 ID
- 请求状态
- 响应耗时
- 错误信息
- 输入长度
- 输出长度
- token 消耗，可选

### 4.6.6 Prompt 模板管理

功能描述：
管理员可以维护系统使用的 Prompt 模板。

功能包括：

- 新增模板
- 编辑模板
- 启用或停用模板
- 查看模板版本
- 回滚历史版本

------

## 5. 非功能需求

## 5.1 性能需求

1. 简历上传接口响应时间应控制在 3 秒以内，不包含后台 AI 分析时间。
2. 简历解析任务建议异步处理。
3. 单次 AI 分析任务建议在 60 秒内返回结果；如耗时较长，应提供任务状态轮询机制。
4. 列表查询接口应支持分页。
5. 高频查询字段应建立数据库索引。

## 5.2 安全需求

1. 用户密码必须加密存储。
2. DeepSeek API Key 不得暴露到前端。
3. 用户只能访问自己的简历、岗位描述和分析报告。
4. 管理员操作需要权限校验和操作日志。
5. 上传文件需要进行类型和大小校验。
6. 防止恶意文件上传。
7. 防止 SQL 注入、XSS、CSRF 等常见安全问题。
8. 敏感信息在日志中应脱敏。

## 5.3 隐私与合规需求

简历通常包含姓名、电话、邮箱、教育经历、工作经历等个人敏感信息，因此系统必须重视隐私保护。

要求：

1. 明确告知用户数据用途。
2. 未经用户授权，不得将简历内容用于其他用途。
3. 支持用户删除个人简历和分析记录。
4. 日志中不得明文记录完整简历内容。
5. 管理员访问用户简历需有权限控制和审计记录。
6. 对外部 AI API 传输数据前，应在产品协议中明确说明。

## 5.4 可维护性需求

1. 前后端代码结构清晰，模块化设计。
2. AI 调用逻辑与业务逻辑解耦。
3. Prompt 模板可配置、可版本化。
4. 数据库迁移应使用版本管理工具。
5. 关键业务逻辑应编写单元测试。
6. 异常日志应便于定位问题。

## 5.5 可扩展性需求

1. 后续可扩展支持更多大模型 API。
2. 后续可支持更多简历格式。
3. 后续可增加在线简历编辑与导出功能。
4. 后续可增加多语言简历分析。
5. 后续可增加企业招聘端。
6. 后续可增加付费会员体系。

------

## 6. 技术架构需求

## 6.1 技术选型建议

### 前端

可选方案：

- Python Streamlit，适合快速原型和内部工具。
- Python Dash，适合数据展示型应用。
- Flask/Jinja2，适合轻量级服务端渲染页面。
- FastAPI + Jinja2，适合轻量 Python 全栈实现。

一期如果强调快速开发，可优先采用 Streamlit 或 FastAPI + Jinja2。
如果后续计划发展为正式产品，可在后续版本将前端拆分为独立前端项目。

### 后端

建议使用：

- FastAPI
- SQLAlchemy
- Pydantic
- Alembic
- Celery 或 RQ，用于异步任务
- Redis，用于任务队列和缓存，可选
- Uvicorn / Gunicorn，用于 Python Web 服务部署

### 数据库

- PostgreSQL

### 文件存储

一期可使用服务器本地挂载目录进行文件存储，并通过 Docker Volume 或宿主机目录挂载实现数据持久化。

后续可扩展为对象存储，如 MinIO、S3 兼容存储或云厂商 OSS。

### AI API

- DeepSeek API

后端统一封装模型调用层，避免业务代码直接依赖具体模型供应商。

### 环境管理与部署

项目采用 Docker 管理运行环境，使用 Docker Compose 编排前端、后端、数据库、缓存、异步任务和反向代理等服务。

Docker 化的目标：

1. 保证开发、测试和生产环境一致。
2. 降低 Python 依赖、数据库版本和系统环境差异带来的部署问题。
3. 支持在个人云服务器上快速部署和迁移。
4. 便于后续扩展 Redis、Celery Worker、Nginx、HTTPS 证书等组件。
5. 支持通过环境变量管理不同部署环境的配置。

------

## 6.2 系统架构

建议架构如下：

1. 前端页面层
   - 用户登录注册页面
   - 简历上传页面
   - 岗位描述管理页面
   - 分析报告页面
   - 历史记录页面
   - 管理后台页面
2. 后端 API 层
   - 用户认证接口
   - 简历管理接口
   - 岗位描述接口
   - 分析任务接口
   - 报告查询接口
   - 管理后台接口
3. 业务服务层
   - 用户服务
   - 简历服务
   - 岗位服务
   - 分析服务
   - 报告服务
   - 管理服务
4. AI 服务层
   - Prompt 构建
   - DeepSeek API 调用
   - 响应解析
   - 结果校验
   - 异常重试
5. 数据层
   - PostgreSQL
   - 文件存储
   - Redis，可选
6. 部署与运维层
   - Docker
   - Docker Compose
   - Nginx 反向代理
   - HTTPS 证书
   - 环境变量配置
   - 数据库备份脚本
   - 应用日志与容器日志

------

## 6.3 Docker 部署架构需求

### 6.3.1 容器服务划分

建议使用 Docker Compose 管理以下服务：

| 服务名称  | 说明                                                  |
| --------- | ----------------------------------------------------- |
| web / app | Python Web 应用服务，运行 FastAPI、Flask 或 Streamlit |
| db        | PostgreSQL 数据库服务                                 |
| redis     | Redis 缓存和任务队列服务，可选但推荐                  |
| worker    | 异步任务服务，用于执行简历解析和 AI 分析任务          |
| nginx     | 反向代理服务，用于转发请求、配置域名和 HTTPS          |
| backup    | 数据库备份服务或定时备份脚本，可选                    |

一期 MVP 可以先使用：

- app
- db
- nginx

当 AI 分析任务较耗时时，再增加：

- redis
- worker

### 6.3.2 开发环境 Docker 要求

开发环境应支持：

1. 使用 `docker compose up` 启动全部依赖服务。
2. 后端代码可通过 volume 挂载，支持本地修改后自动重载。
3. PostgreSQL 使用独立 volume 保存开发数据。
4. `.env.dev` 管理开发环境变量。
5. 支持执行数据库迁移命令。
6. 支持查看容器日志，便于调试。

开发环境建议服务：

- app
- db
- redis，可选

### 6.3.3 生产环境 Docker 要求

生产环境应支持部署到个人云服务器，建议使用 Docker Compose 运行所有核心服务。

生产环境要求：

1. 使用 `.env.prod` 管理生产环境变量。
2. DeepSeek API Key、数据库密码、JWT 密钥等敏感信息不得写入代码仓库。
3. PostgreSQL 数据目录必须持久化。
4. 用户上传的简历文件必须持久化。
5. Nginx 作为公网入口。
6. 后端应用服务不直接暴露到公网。
7. 数据库端口不直接暴露到公网。
8. 支持 HTTPS 访问。
9. 支持容器异常重启策略。
10. 支持日志查看和基础故障排查。

### 6.3.4 生产环境推荐访问链路

用户访问链路：

1. 用户浏览器访问域名。
2. 域名解析到个人云服务器公网 IP。
3. Nginx 接收 80 / 443 端口请求。
4. Nginx 将请求反向代理到 app 容器。
5. app 容器访问 PostgreSQL、Redis 和 DeepSeek API。
6. app 容器读取或写入用户上传文件目录。

生产环境中，只有 Nginx 对外暴露端口，其他服务尽量只在 Docker 内部网络中通信。

### 6.3.5 Docker Compose 服务示意

后续开发时建议提供以下文件：

```text
resume-analyzer/
├── app/
│   ├── main.py
│   ├── api/
│   ├── services/
│   ├── models/
│   ├── schemas/
│   ├── templates/
│   └── static/
├── migrations/
├── nginx/
│   └── nginx.conf
├── scripts/
│   ├── backup_db.sh
│   └── restore_db.sh
├── storage/
│   └── resumes/
├── Dockerfile
├── docker-compose.yml
├── docker-compose.prod.yml
├── requirements.txt
├── .env.example
├── .env.dev
└── .env.prod
```

说明：

- `.env.example` 可以提交到代码仓库，用于说明需要哪些配置。
- `.env.dev` 和 `.env.prod` 不应提交到公开仓库。
- `storage/resumes` 用于保存用户上传的简历文件。
- `migrations` 用于保存数据库迁移文件。
- `scripts` 用于保存备份和恢复脚本。

### 6.3.6 Dockerfile 需求

Dockerfile 应满足：

1. 使用稳定 Python 基础镜像。
2. 安装项目依赖。
3. 设置工作目录。
4. 拷贝项目代码。
5. 设置环境变量。
6. 暴露应用内部端口。
7. 使用非 root 用户运行应用，推荐。
8. 生产环境关闭 debug 模式。

### 6.3.7 环境变量配置需求

系统配置应通过环境变量读取。

关键环境变量包括：

| 变量名             | 说明                    |
| ------------------ | ----------------------- |
| APP_ENV            | 当前环境，dev/test/prod |
| APP_DEBUG          | 是否开启调试模式        |
| APP_SECRET_KEY     | 应用密钥                |
| DATABASE_URL       | PostgreSQL 连接地址     |
| REDIS_URL          | Redis 连接地址          |
| DEEPSEEK_API_KEY   | DeepSeek API Key        |
| DEEPSEEK_BASE_URL  | DeepSeek API 地址       |
| DEEPSEEK_MODEL     | DeepSeek 模型名称       |
| JWT_SECRET_KEY     | JWT 签名密钥            |
| JWT_EXPIRE_MINUTES | JWT 过期时间            |
| UPLOAD_DIR         | 简历上传目录            |
| MAX_UPLOAD_SIZE_MB | 最大上传文件大小        |
| LOG_LEVEL          | 日志级别                |
| ALLOWED_ORIGINS    | 允许跨域来源            |

### 6.3.8 数据持久化需求

生产环境必须持久化以下数据：

1. PostgreSQL 数据文件。
2. 用户上传的简历原始文件。
3. 应用日志，可选。
4. Nginx 日志，可选。
5. 数据库备份文件。

推荐使用 Docker Volume 或宿主机目录挂载。

示例挂载目标：

```text
/opt/resume-analyzer/postgres_data
/opt/resume-analyzer/uploads
/opt/resume-analyzer/logs
/opt/resume-analyzer/backups
```

### 6.3.9 数据备份与恢复需求

由于系统包含用户简历和分析报告，生产环境需要提供基础备份能力。

备份要求：

1. PostgreSQL 数据库支持手动备份。
2. PostgreSQL 数据库支持定时备份。
3. 用户上传文件目录支持定期归档。
4. 备份文件应按日期命名。
5. 至少保留最近 7 天备份，可根据服务器容量调整。
6. 提供数据库恢复脚本或恢复说明。

备份内容包括：

- 数据库表数据
- 用户上传简历文件
- 必要配置说明，不包含明文密钥

### 6.3.10 云服务器部署需求

个人云服务器建议配置：

| 配置项   | MVP 建议              |
| -------- | --------------------- |
| CPU      | 2 核及以上            |
| 内存     | 2 GB 起步，推荐 4 GB  |
| 磁盘     | 40 GB 起步            |
| 操作系统 | Ubuntu LTS            |
| 网络     | 开放 80、443、22 端口 |
| 域名     | 推荐绑定域名          |

服务器需要安装：

- Docker
- Docker Compose Plugin
- Git
- Nginx，可选择容器化部署
- Certbot 或使用 Nginx Proxy Manager 管理 HTTPS，可选

生产部署步骤建议：

1. 购买云服务器并安装 Ubuntu LTS。
2. 配置安全组，只开放必要端口。
3. 安装 Docker 和 Docker Compose。
4. 拉取项目代码。
5. 创建 `.env.prod` 文件。
6. 创建数据挂载目录。
7. 启动 Docker Compose 生产配置。
8. 执行数据库迁移。
9. 配置域名解析。
10. 配置 Nginx 反向代理和 HTTPS。
11. 验证登录、上传、分析和后台功能。
12. 配置数据库备份策略。

### 6.3.11 发布与更新需求

系统后续发布到个人云服务器后，应支持平滑更新。

基础发布流程：

1. 本地完成开发和测试。
2. 提交代码到 Git 仓库。
3. 服务器拉取最新代码。
4. 重新构建 Docker 镜像。
5. 停止旧容器。
6. 启动新容器。
7. 执行数据库迁移。
8. 检查服务状态和日志。

后续可扩展为自动化发布流程，例如 GitHub Actions 或 Gitee CI。

### 6.3.12 容器日志需求

系统应支持通过 Docker 查看日志。

日志要求：

1. 应用日志输出到标准输出，便于 `docker logs` 查看。
2. 生产环境可按日期保存关键日志。
3. 错误日志需包含请求 ID、用户 ID、任务 ID 和错误阶段。
4. 日志中不得输出完整简历内容、密码、API Key、JWT 等敏感信息。

### 6.3.13 Docker 安全要求

1. 生产环境不得开启 Debug 模式。
2. 数据库端口不得直接暴露到公网。
3. Redis 端口不得直接暴露到公网。
4. `.env.prod` 不得提交到代码仓库。
5. API Key 和数据库密码必须通过环境变量注入。
6. 上传目录应限制可执行权限。
7. 容器建议使用非 root 用户运行。
8. 定期更新基础镜像和依赖包。

------

## 7. 数据库需求设计

## 7.1 用户表 users

| 字段            | 类型             | 说明            |
| --------------- | ---------------- | --------------- |
| id              | UUID / BIGSERIAL | 用户 ID         |
| email           | VARCHAR          | 邮箱，唯一      |
| password_hash   | VARCHAR          | 密码哈希        |
| nickname        | VARCHAR          | 昵称            |
| phone           | VARCHAR          | 手机号          |
| target_position | VARCHAR          | 目标岗位        |
| work_years      | NUMERIC          | 工作年限        |
| target_city     | VARCHAR          | 目标城市        |
| role            | VARCHAR          | user/admin      |
| status          | VARCHAR          | active/disabled |
| created_at      | TIMESTAMP        | 创建时间        |
| updated_at      | TIMESTAMP        | 更新时间        |

## 7.2 简历表 resumes

| 字段            | 类型             | 说明                           |
| --------------- | ---------------- | ------------------------------ |
| id              | UUID / BIGSERIAL | 简历 ID                        |
| user_id         | UUID / BIGINT    | 用户 ID                        |
| title           | VARCHAR          | 简历名称                       |
| file_name       | VARCHAR          | 原始文件名                     |
| file_type       | VARCHAR          | 文件类型                       |
| file_path       | VARCHAR          | 文件路径                       |
| raw_text        | TEXT             | 解析后的原始文本               |
| structured_data | JSONB            | 结构化简历内容                 |
| version         | INTEGER          | 版本号                         |
| status          | VARCHAR          | uploaded/parsed/failed/deleted |
| created_at      | TIMESTAMP        | 创建时间                       |
| updated_at      | TIMESTAMP        | 更新时间                       |

## 7.3 岗位描述表 job_descriptions

| 字段             | 类型             | 说明           |
| ---------------- | ---------------- | -------------- |
| id               | UUID / BIGSERIAL | 岗位 ID        |
| user_id          | UUID / BIGINT    | 用户 ID        |
| title            | VARCHAR          | 岗位名称       |
| company_name     | VARCHAR          | 公司名称       |
| industry         | VARCHAR          | 行业           |
| location         | VARCHAR          | 工作地点       |
| description_text | TEXT             | 岗位描述原文   |
| structured_data  | JSONB            | 结构化岗位信息 |
| created_at       | TIMESTAMP        | 创建时间       |
| updated_at       | TIMESTAMP        | 更新时间       |

## 7.4 分析任务表 analysis_tasks

| 字段               | 类型             | 说明                           |
| ------------------ | ---------------- | ------------------------------ |
| id                 | UUID / BIGSERIAL | 任务 ID                        |
| user_id            | UUID / BIGINT    | 用户 ID                        |
| resume_id          | UUID / BIGINT    | 简历 ID                        |
| job_description_id | UUID / BIGINT    | 岗位 ID                        |
| task_type          | VARCHAR          | score/match/full               |
| status             | VARCHAR          | pending/running/success/failed |
| error_message      | TEXT             | 错误信息                       |
| started_at         | TIMESTAMP        | 开始时间                       |
| finished_at        | TIMESTAMP        | 完成时间                       |
| created_at         | TIMESTAMP        | 创建时间                       |

## 7.5 分析报告表 analysis_reports

| 字段               | 类型             | 说明          |
| ------------------ | ---------------- | ------------- |
| id                 | UUID / BIGSERIAL | 报告 ID       |
| task_id            | UUID / BIGINT    | 分析任务 ID   |
| user_id            | UUID / BIGINT    | 用户 ID       |
| resume_id          | UUID / BIGINT    | 简历 ID       |
| job_description_id | UUID / BIGINT    | 岗位 ID       |
| total_score        | NUMERIC          | 简历总分      |
| match_score        | NUMERIC          | 岗位匹配度    |
| report_data        | JSONB            | 完整报告 JSON |
| summary            | TEXT             | 报告摘要      |
| created_at         | TIMESTAMP        | 创建时间      |

## 7.6 API 调用日志表 api_call_logs

| 字段            | 类型             | 说明                    |
| --------------- | ---------------- | ----------------------- |
| id              | UUID / BIGSERIAL | 日志 ID                 |
| user_id         | UUID / BIGINT    | 用户 ID                 |
| task_id         | UUID / BIGINT    | 任务 ID                 |
| provider        | VARCHAR          | 模型供应商，如 deepseek |
| model_name      | VARCHAR          | 模型名称                |
| scene           | VARCHAR          | 调用场景                |
| request_length  | INTEGER          | 输入长度                |
| response_length | INTEGER          | 输出长度                |
| latency_ms      | INTEGER          | 响应耗时                |
| status          | VARCHAR          | success/failed          |
| error_message   | TEXT             | 错误信息                |
| created_at      | TIMESTAMP        | 创建时间                |

## 7.7 Prompt 模板表 prompt_templates

| 字段       | 类型             | 说明            |
| ---------- | ---------------- | --------------- |
| id         | UUID / BIGSERIAL | 模板 ID         |
| name       | VARCHAR          | 模板名称        |
| scene      | VARCHAR          | 使用场景        |
| content    | TEXT             | Prompt 内容     |
| version    | INTEGER          | 版本号          |
| status     | VARCHAR          | active/inactive |
| created_by | UUID / BIGINT    | 创建人          |
| created_at | TIMESTAMP        | 创建时间        |
| updated_at | TIMESTAMP        | 更新时间        |

------

## 8. 接口需求设计

## 8.1 用户认证接口

### POST /api/auth/register

用户注册。

请求参数：

- email
- password
- nickname

返回结果：

- user_id
- email
- token

### POST /api/auth/login

用户登录。

请求参数：

- email
- password

返回结果：

- access_token
- user_info

### POST /api/auth/logout

用户退出登录。

------

## 8.2 简历接口

### POST /api/resumes/upload

上传简历文件。

请求参数：

- file
- title

返回结果：

- resume_id
- parse_status

### GET /api/resumes

获取简历列表。

### GET /api/resumes/{resume_id}

获取简历详情。

### DELETE /api/resumes/{resume_id}

删除简历。

------

## 8.3 岗位描述接口

### POST /api/jobs

创建岗位描述。

### GET /api/jobs

获取岗位描述列表。

### GET /api/jobs/{job_id}

获取岗位描述详情。

### DELETE /api/jobs/{job_id}

删除岗位描述。

------

## 8.4 分析接口

### POST /api/analysis/tasks

创建分析任务。

请求参数：

- resume_id
- job_description_id
- analysis_type

返回结果：

- task_id
- status

### GET /api/analysis/tasks/{task_id}

查询分析任务状态。

### GET /api/analysis/reports/{report_id}

查询分析报告详情。

### GET /api/analysis/reports

查询历史报告列表。

------

## 8.5 管理后台接口

### GET /api/admin/users

查询用户列表。

### GET /api/admin/analysis-tasks

查询分析任务列表。

### GET /api/admin/api-logs

查询 API 调用日志。

### GET /api/admin/prompt-templates

查询 Prompt 模板列表。

### POST /api/admin/prompt-templates

新增 Prompt 模板。

### PUT /api/admin/prompt-templates/{template_id}

更新 Prompt 模板。

------

## 9. 页面需求

## 9.1 用户端页面

### 9.1.1 登录注册页

功能：

- 用户注册
- 用户登录
- 错误提示

### 9.1.2 首页 / 工作台

展示内容：

- 已上传简历数量
- 历史分析次数
- 最近一次分析结果
- 快捷入口：上传简历、创建岗位、开始分析

### 9.1.3 简历上传页

功能：

- 上传 PDF、DOCX、TXT
- 输入简历名称
- 查看解析状态
- 查看解析文本

### 9.1.4 岗位描述页

功能：

- 新建岗位描述
- 编辑岗位描述
- 查看历史岗位描述

### 9.1.5 简历分析页

功能：

- 选择简历
- 选择岗位描述
- 发起分析
- 查看分析进度

### 9.1.6 分析报告页

展示内容：

- 总分
- 匹配度
- 优势总结
- 问题诊断
- 关键词覆盖分析
- 修改建议
- 改写示例

### 9.1.7 历史记录页

功能：

- 查看历史报告
- 按简历筛选
- 按岗位筛选
- 按时间排序

### 9.1.8 个人中心页

功能：

- 查看和修改个人信息
- 查看账号状态
- 删除账号数据入口，可选

------

## 9.2 管理后台页面

### 9.2.1 管理后台首页

展示内容：

- 用户总数
- 简历总数
- 分析任务总数
- 今日 API 调用次数
- 失败任务数

### 9.2.2 用户管理页

功能：

- 用户列表
- 用户搜索
- 用户状态管理

### 9.2.3 简历管理页

功能：

- 简历上传记录查看
- 解析状态查看
- 异常记录查看

### 9.2.4 分析任务页

功能：

- 查看任务状态
- 查看失败原因
- 按用户、时间、状态筛选

### 9.2.5 API 日志页

功能：

- 查看 API 调用记录
- 查看调用耗时
- 查看错误信息
- 按场景筛选

### 9.2.6 Prompt 模板管理页

功能：

- 查看模板列表
- 新建模板
- 编辑模板
- 启停模板
- 版本管理

------

## 10. 核心分析报告字段设计

分析报告建议使用 JSONB 存储，结构示例如下：

```json
{
  "total_score": 82,
  "match_score": 76,
  "score_breakdown": {
    "completeness": 18,
    "structure": 13,
    "professional_expression": 16,
    "experience_quality": 17,
    "skill_match": 11,
    "quantified_results": 7
  },
  "strengths": [
    "项目经历较完整",
    "技术栈与岗位要求有一定重合",
    "工作经历时间线清晰"
  ],
  "weaknesses": [
    "部分项目缺少量化结果",
    "岗位关键词覆盖不足",
    "技能模块缺少熟练程度区分"
  ],
  "matched_keywords": ["Python", "SQL", "数据分析"],
  "missing_keywords": ["FastAPI", "PostgreSQL", "LLM"],
  "suggestions": [
    {
      "priority": "high",
      "module": "项目经历",
      "problem": "项目成果不够具体",
      "suggestion": "补充项目规模、使用技术、个人职责和最终结果"
    }
  ],
  "rewrite_examples": [
    {
      "original": "负责后台接口开发",
      "issue": "缺少技术细节和结果",
      "rewritten": "基于 FastAPI 设计并实现用户认证、简历上传和分析任务接口，支持前端完成完整求职分析流程。"
    }
  ],
  "risk_notes": [
    "建议仅补充真实掌握的技能，不要为了匹配岗位而虚构经历。"
  ]
}
```

------

## 11. 权限设计

## 11.1 普通用户权限

普通用户可以：

- 管理自己的简历
- 管理自己的岗位描述
- 发起自己的分析任务
- 查看自己的分析报告
- 修改自己的个人信息

普通用户不可以：

- 查看其他用户数据
- 查看系统 API 调用日志
- 修改 Prompt 模板
- 访问管理后台

## 11.2 管理员权限

管理员可以：

- 查看用户列表
- 查看系统统计数据
- 查看分析任务状态
- 查看 API 调用日志
- 管理 Prompt 模板
- 管理异常数据

管理员访问用户简历原文时，需要记录审计日志。

------

## 12. 任务状态设计

分析任务状态包括：

| 状态     | 说明         |
| -------- | ------------ |
| pending  | 等待执行     |
| running  | 正在分析     |
| success  | 分析成功     |
| failed   | 分析失败     |
| canceled | 已取消，可选 |

任务失败时需要记录：

- 失败阶段
- 错误类型
- 错误信息
- 是否可重试

------

## 13. 错误提示需求

常见错误提示：

| 场景           | 用户提示                                  |
| -------------- | ----------------------------------------- |
| 文件格式不支持 | 当前仅支持 PDF、DOCX、TXT 格式简历        |
| 文件过大       | 文件大小超过限制，请上传 10 MB 以内的文件 |
| 简历解析失败   | 简历解析失败，请尝试粘贴文本内容          |
| 岗位描述为空   | 请先输入目标岗位描述                      |
| AI 分析失败    | 当前分析服务暂时不可用，请稍后重试        |
| 登录过期       | 登录状态已过期，请重新登录                |
| 无访问权限     | 你没有权限访问该内容                      |

------

## 14. 日志与监控需求

系统需要记录以下日志：

1. 用户登录日志
2. 文件上传日志
3. 简历解析日志
4. 分析任务日志
5. DeepSeek API 调用日志
6. 系统异常日志
7. 管理员操作日志

监控指标包括：

- 每日活跃用户数
- 每日简历上传数量
- 每日分析任务数量
- AI 调用成功率
- AI 平均响应耗时
- 分析任务失败率
- API 调用成本，可选

------

## 15. 验收标准

## 15.1 功能验收

1. 用户可以完成注册、登录和退出登录。
2. 用户可以上传 PDF、DOCX、TXT 格式简历。
3. 系统可以解析简历文本并保存。
4. 用户可以创建岗位描述。
5. 用户可以选择简历和岗位发起分析。
6. 系统可以调用 DeepSeek API 并返回分析结果。
7. 用户可以查看完整分析报告。
8. 用户可以查看历史分析记录。
9. 管理员可以查看用户、任务和 API 调用日志。
10. Prompt 模板可以被后台维护。

## 15.2 性能验收

1. 常规页面加载时间不超过 3 秒。
2. 简历列表查询支持分页。
3. 单个普通简历的完整分析任务可在合理时间内完成。
4. AI 调用失败时系统不会阻塞主流程。

## 15.3 安全验收

1. 未登录用户不能访问业务接口。
2. 普通用户不能访问他人简历和报告。
3. API Key 不会出现在前端代码或接口响应中。
4. 密码不会明文存储。
5. 上传文件会进行类型和大小校验。
6. 管理员操作有日志记录。

------

## 16. 里程碑计划

### 第一阶段：需求确认与原型设计

产出：

- 项目需求文档
- 页面原型
- 数据库初版设计
- 接口清单

### 第二阶段：基础框架搭建

产出：

- FastAPI 后端项目结构
- PostgreSQL 数据库连接
- 用户认证模块
- 基础前端页面
- 文件上传能力

### 第三阶段：简历与岗位管理

产出：

- 简历上传与解析
- 简历列表与详情
- 岗位描述管理
- 结构化数据存储

### 第四阶段：AI 分析能力接入

产出：

- DeepSeek API 封装
- Prompt 模板
- 简历评分功能
- 岗位匹配功能
- 分析报告生成

### 第五阶段：管理后台与日志

产出：

- 用户管理
- 分析任务管理
- API 调用日志
- Prompt 模板管理

### 第六阶段：测试与上线

产出：

- 功能测试
- 安全测试
- 性能优化
- 部署文档
- 上线版本

------

## 17. 风险与应对

| 风险                     | 影响             | 应对措施                                      |
| ------------------------ | ---------------- | --------------------------------------------- |
| 简历解析准确率不足       | 影响后续分析质量 | 支持用户手动修正文本；结合规则解析与 AI 解析  |
| AI 输出不稳定            | 报告质量不一致   | 使用结构化 Prompt；增加响应校验和重试机制     |
| API 调用成本过高         | 增加运营成本     | 控制输入长度；缓存分析结果；限制免费调用次数  |
| 用户隐私风险             | 法律和信任风险   | 数据脱敏、权限控制、删除机制、隐私说明        |
| 大文件或复杂格式导致失败 | 用户体验下降     | 限制格式和大小；提供文本粘贴兜底方案          |
| Prompt 被注入攻击        | 影响模型输出安全 | 对用户输入进行边界隔离；Prompt 中明确系统规则 |

------

## 18. 一期最小可行版本 MVP 建议

为了快速落地，建议一期 MVP 先实现以下核心功能：

1. 用户注册登录
2. 简历上传和文本解析
3. 岗位描述输入
4. 调用 DeepSeek API 生成简历评分和岗位匹配报告
5. 历史分析记录
6. 管理员查看 API 调用日志

MVP 可暂缓实现：

- 复杂后台权限系统
- Prompt 版本回滚
- 多模型切换
- 在线简历编辑器
- 报告导出 PDF
- 付费系统

------

## 19. 后续扩展方向

1. 支持根据岗位一键生成定制版简历。
2. 支持生成求职信。
3. 支持模拟面试问题生成。
4. 支持面试回答质量分析。
5. 支持多语言简历优化。
6. 支持简历 PDF 导出。
7. 支持 ATS 友好度检测。
8. 支持求职进度管理。
9. 支持企业端批量简历筛选。
10. 支持多模型切换，如 DeepSeek、OpenAI、Claude、本地模型等。

------

## 20. 结论

本项目旨在构建一套面向求职者的智能简历分析系统。系统以 Python 技术栈为核心，使用 PostgreSQL 作为主要数据库，并通过 DeepSeek API 提供 AI 分析能力。

一期建设重点应放在简历上传解析、岗位匹配分析、AI 报告生成和历史记录管理上。系统设计时应特别关注用户隐私、模型输出稳定性、Prompt 可维护性和后续扩展能力。